# AGENTS.md

本文件约定 AI 智能体在本仓库中的工作方式。

## 运行方式

只用这一条命令运行项目：

```bash
npm run dev
```

不要为了"跑起来看看"而自己写启动脚本、探针脚本或后台任务去拉起 Electron。

## 测试分工：用户跑，智能体读日志

**智能体不跑测试，只负责写测试和读日志。**

- 需要"跑一次才知道"的信息（登录、Cloudflare、代理、页面结构、真实会话里的行为），
  一律写成 `tools/diag/` 下的自包含探针，**由用户运行、用户操作**。
- 探针把过程写进日志文件。**智能体读日志、引用关键行、说明含义**，再决定下一步。
- 探针的长相、日志位置和硬性要求（`fs.appendFileSync` 而不是写流、cookie 只记名字和长度、
  日志落在 `%TEMP%` 不落仓库、别重复注册同一个 webRequest 监听器……）
  都在 **`tools/diag/README.md`** —— 动手写探针之前先读它。

**唯一允许智能体直接跑的 Electron 是 `npm run build`（只打包，不联网）。**

自己拉起 Electron 去跑测试 —— 即使设了 `show: false`、即使用户一个键都不用按 ——
**同样是违规的**。它用的是用户真实的 `persist:chatgpt` 分区，还会从同一个 IP
反复打真实第三方服务；`auth.openai.com` 就是被这样打到过「你的会话已结束」的限流页，
而那一页看起来和登录失败一模一样。

交付标准是**「构建通过，等待测试」**，不是「已验证」。构建通过 ≠ 功能可用。

## 不要主动编辑 AGENTS.md

**只有在用户明确要求时，才动这个文件。**

自己觉得某条经验值得记下来、顺手写进去，即使内容完全正确、即使上一轮刚刚踩过这个坑，
也不行 —— 用户没让改就是没让改。

想记录的经验**先说出来**，由用户决定要不要落盘、落到哪里。

## 常用命令

```bash
npm run dev          # 开发模式：Vite dev server + Electron（带 HMR）
npm run build        # typecheck + 打包到 out/
npm run typecheck    # 只做类型检查
npm run dist:win     # 打 Windows 安装包
```

改完代码至少让 `npm run build` 通过再交付。
