# AGENTS.md

本文件约定 AI 智能体在本仓库中的工作方式。

## 运行方式

**只用这一条命令运行项目：**

```bash
npm run dev
```

不要为了"跑起来看看"而自己写启动脚本、探针脚本或后台任务去拉起 Electron。

## 测试分工

| 角色 | 负责 |
| --- | --- |
| 智能体 | 写代码、`npm run build`（内含 typecheck）通过、说明改了什么 |
| 用户 | 实际运行项目、交互测试、把结果（截图 / 报错 / 现象）反馈回来 |

规则：

- **不要替用户做交互测试**，也不要写自动化的界面验证脚本。
- 在用户反馈测试结果之前，**不要声称功能"已验证"或"能用"**。
  构建通过 ≠ 功能可用，只能说"构建通过，等待测试"。
- 用户贴回结果后，再根据结果修改。有报错就把完整报错贴回来。

## 常用命令

```bash
npm run dev          # 开发模式：Vite dev server + Electron（带 HMR）
npm run build        # typecheck + 打包到 out/
npm run typecheck    # 只做类型检查（node + web 两个 TS 工程）
npm run electron:install   # 预下载 Electron 二进制（走镜像）
npm run dist:win     # 打 Windows 安装包
```

改完代码至少让 `npm run build` 通过再交付。

## 环境注意事项

### ELECTRON_RUN_AS_NODE

如果终端是从 DSH Desktop 里拉起来的，它会注入 `ELECTRON_RUN_AS_NODE=1`。
这会让 Electron **以纯 Node 模式启动**，主进程立刻崩在：

```
TypeError: Cannot read properties of undefined (reading 'isPackaged')
```

这个变量有坑：`Get-ChildItem env:` 看不到它，`$env:ELECTRON_RUN_AS_NODE = $null`
也清不掉（PowerShell 自己显示为空，子进程拿到的仍是 `1`）。

用户请在自己开的普通终端里运行 `npm run dev` 即可。详见 README 的排错章节。

### 网络

本机无法访问 GitHub，`.npmrc` 已把 Electron 二进制指向 npmmirror。
注意镜像只在 npm 上下文生效（`@electron/get` 优先读 `npm_config_electron_mirror`）。

## 代码约定

- 三个进程：`src/main`（Node）、`src/preload`（contextBridge）、`src/renderer`（沙箱网页）。
- **新增 IPC 通道必须三处同步改**：`src/shared/types.ts` 的 `IpcChannels` 与 `AppApi`、
  `src/main/index.ts` 的 handler、`src/preload/index.ts` 的实现。
  `AppApi` 是唯一真源，签名不一致会编译报错。
- 主进程的 IPC handler 需用 `fromAppWindow(event)` 校验发送方 —— 被嵌入的第三方页面
  不能操纵原生能力。
- 不要给嵌入的 chatgpt.com 页面加 preload 或放开 `sandbox`。

## 界面上的硬约束

嵌入的 chatgpt.com 是原生 `WebContentsView`，**不是 DOM 节点**，永远盖在渲染层之上：

- `.stage__slot` 上方**不能有任何浮层**（下拉、tooltip、弹窗都会被挡住）。
  需要时先调 `window.api.setEmbedVisible(false)`。
  **设置弹窗就是这么做的**（`App.tsx` 里监听 `settingsOpen` 的 effect），
  任何以后新增的全屏浮层都要照做，否则会被原生视图盖住。
- 槽位必须是直角，原生视图无法按 `border-radius` 裁剪。
- 槽位的坐标由 `getBoundingClientRect()` 上报，本身就是 DIP，**不要乘 devicePixelRatio**。

## 数据

对话记录存在 SQLite：`<userData>/conversations.db`
（用 Node 内置的 `node:sqlite`，不是 `better-sqlite3`，因此没有原生依赖、无需 ABI 重编译）。

执行记录也在同一个库的 `executions` 表，**主键是 ChatGPT 的 assistant 消息 id** ——
这是「跑过的命令不再跑」的幂等依据，不要改成自增 id 或时间戳。

## 设置与代理

- **代理只作用于嵌入视图的 session，绝不全局。**
  `session.fromPartition(EMBED_PARTITION).setProxy(...)`。
  **不要用 `app.commandLine.appendSwitch('proxy-server', ...)`** —— 那会把整个应用的连接
  一起拖进代理，包括明确不该走代理的东西（以后的 SSH）。
- **`EMBED_PARTITION` 必须留在 `shared/types.ts`。** 它被两处使用：`embed.ts` 建视图时、
  `index.ts` 设代理时。两边写不一致的话，代理会**静默地设到一个没人用的 session 上** ——
  不报错、也不生效，最难查的那类 bug。`persist:chatgpt` 这个字面量只允许出现在常量定义处。
- **Electron 不持久化 session 代理**，所以每次启动都要从数据库重新应用，而且必须在
  `createWindow()` **之前** —— 否则页面发出的第一批请求已经走了直连。
- 改完代理要 `closeAllConnections()` **并且重载页面**：已建立的连接仍走旧路由，
  不重载用户会以为设置没生效。
- 用户很可能只输入 `127.0.0.1:7890`。`normalizeProxy()` 会自动补 `http://`，
  **不要因为缺 scheme 就拒绝**，并要把归一化后的值回显给用户。

## SSH

- **用 `ssh2`（纯 JavaScript），不要改用系统 `ssh.exe`。** `ssh.exe` 只从控制台读密码，
  没法程序化喂给它；`ssh2` 支持密码认证 + PTY shell。
  它的原生加速模块（`cpu-features`/`nan`）是**可选**的，npm 的 allow-scripts 门会跳过编译，
  纯 JS 路径照常工作 —— 所以**不需要 ABI 重编译**，`npmRebuild: false` 保持关闭。
- **`ssh2` 是 `dependencies` 里的运行时依赖，会被 `externalizeDepsPlugin` 外部化，
  因此 `electron-builder.yml` 的 `files` 必须包含 `node_modules/**`。**
  以前这里只打包 `out/**`（因为当时零运行时依赖），删掉那一行会让打包后的应用缺依赖。
- **SSH 用的代理和 ChatGPT 网页的代理是两个独立设置，不要合并。**
  - `embedProxy` 设在嵌入页面的 Electron **session** 上。
  - `sshProxy` 是**本应用自己拨号**用的，`ssh2` 走 Node 原生 socket，
    **session 代理对它毫无作用** —— 这就是必须分开的原因，不是重复配置。
  - 每个主机还能在连接表单里单独覆盖（`SshHost.proxy`）；**空值表示跟随 `sshProxy`**。
    目前没有"全局设了代理但某台主机强制直连"的办法，这个三态不值得为它加 UI。
- **`connectViaHttpProxy()` 自己实现 HTTP CONNECT**（`http.request({ method: 'CONNECT' })`
  拿到 socket 后交给 ssh2 的 `sock` 选项）。这就是 Electron 的 session 代理帮不上忙的后果：
  隧道得自己开。**已实测**：本地起一个真 CONNECT 代理 + echo 服务，隧道建立、数据往返、
  代理确实被调用都通过。
  - **只支持 `http://` / `https://`（CONNECT），不支持 SOCKS。** 收到 `socks5://` 要给出
    明确说明而不是让它挂到超时 —— Clash 这类代理的混合端口同时讲 HTTP，填那个即可。
  - 地址**可以省略 scheme**（`normalizeProxy` 会补），用户不会老老实实写 `http://`。
  - 隧道建立后必须 `socket.setTimeout(0)`，否则 CONNECT 的超时会跟着 socket 走，
    在握手途中把它掐掉。
  - 代理是异步拨号的，所以 `client.connect()` 挪进了 async 块；
    **用 `this.client !== client` 判断是否已被用户断开**，避免连接建在一个已经废弃的会话上。
- **密码必须经 `safeStorage` 加密后再入库**（Windows 上是 DPAPI，绑定当前用户账户，
  数据库文件被拷走也没用）。`safeStorage.isEncryptionAvailable()` 为 false 时
  **宁可不保存也不要明文写库**，让用户每次重输。
  空字符串是贯穿全流程的"没有保存密码"信号；`upsertSshHost` 里空 secret 表示**保留原值**，
  这样不重输密码地重连不会把已存的密码抹掉。
- **`connect()` 立刻返回 `connecting`，结果通过 `onChanged` 推送。** 握手可能耗时 20 秒，
  阻塞 IPC 回复会把发起它的对话框卡住。
- **终端面板靠 `SshState.attached` 在本地终端和 SSH 之间切换。** 失败后仍保持 attached，
  这样错误信息留在屏幕上可读，而不是一闪而过。
- **`hostVerifier: () => true` 是有意的临时决定** —— 还没有 known_hosts 界面，
  直接拒绝未知主机指纹会让功能完全不可用。**这意味着目前不防中间人攻击。**
  以后要做指纹确认时，改这里而不是在别处打补丁。
- 远端输出按 `\r` 和 `\n` 都断行：没有终端模拟器去移动光标，
  把裸 `\r`（原地重绘）当成"覆盖当前行"只会丢输出。ANSI 转义序列直接剥掉。

### 保存的主机与快速切换

- **主机是持久保存的**（`ssh_hosts` 表）：每次点「连接」都会 upsert，所以"连过一次"就等于"记住了"。
  排序是 `updated_at DESC`，最近用的在最上面。
  - **空 secret 表示"沿用已存密码"**（`upsertSshHost` 里的 `CASE WHEN excluded.secret = ''`）。
    这条是"不重输密码就能切回去"的全部依据，改动它等于把一键切换废掉。
    **改主机地址/端口/代理却不重输密码时，密码必须留着。**
  - `hasPassword` 是**派生**的（`secret !== ''`），真正的密文永远不进渲染层 —— 别把它塞进 `SshHost`。
- **`SshState.hostId` 是"当前在哪台"的唯一身份，不要按地址比对。**
  同一台机器完全可以存两条不同用户/端口的记录，按地址比对会让两行同时高亮（或者都不亮）。
- **快速切换列表放在终端面板的常规流里，不做浮层。** 面板在嵌入页面的**左边**，
  而嵌入页面是永远盖在 DOM 之上的原生视图 —— 挂在头部的下拉框一旦溢出这一列就会被它吞掉。
  用占位而不是浮层，是唯一不会坏的布局。
- **头部按钮数量是有预算的**（窄的时候只有 220px）。所以「断开」被挪进了列表的页脚，
  头部在任何状态下都保持三个按钮。加按钮前先想想能不能放进列表里。
- **没有已存密码的主机，必须打开表单预填，而不是直接 `connectSsh`。**
  后者会先把面板翻成 error 状态、再报「请输入密码」——
  **你把正在看的记录弄丢了，才被告知需要输密码**。这是切换列表里最容易写错的一处。
- 点当前已经在的那一行不做任何事：重连等于白白掐掉一个正在工作的会话。
- **切换主机会走一次完整的 teardown + 握手**，这期间后端是 null，所以本地探测会跑一遍、
  提示词短暂变回本机那份。这是**正确的**：握手期间远端确实还没接管，
  别为了让这段安静下来而"保持"上一个后端。
  用户看不到混乱 —— 镜像只在后端非 null 时才写进 SSH 面板，
  所以本机探测的输出不会出现在"正在连接…"的 SSH 面板里。

### 每台机器的补充说明（拼接在提示词后面）

- **说明是"每台机器一份"的**：本地机器存在 `settings` 的 `localTerminalNotes`，
  每台 SSH 主机存在 `ssh_hosts.note` 列上。**跟着后端走，不跟着界面走** ——
  连上哪台就编辑哪台的。放到 `ssh_hosts` 而不是单独建表，是为了删主机时说明跟着一起没。
- **`environmentScope` 必须和 `environment` 在同一处、同一次快照里赋值。**
  两者分开算的话，会出现"用户在编辑 A 的说明、模型被告知的是 B 的"这种窗口 ——
  而这类错配没有任何报错，只会让模型拿着别人的项目路径去干活。
- **说明必须放在提示词最后，并且显式声明优先级高于通用约定。** 两条都不能省：
  - 放最后是因为越靠近结尾的指令权重越高，而这是整份提示词里最具体的一条。
  - `**它与上面的通用约定冲突时，以这里为准**` 这句是**功能能否生效的关键**。
    上面的通用建议（"用 apt-get"、"中间结果放变量别写文件"）平均来说是对的，
    但在某台具体机器上可能恰好不对 —— 那正是用户写说明的原因。
    没有这句，模型会把说明当成"又一条建议"，然后继续按通用规则来，
    用户看到的现象就是"**设了没用**"。
- **说明为空时一个字都不要加**（`notesSection()` 直接返回空数组）。
  留一个空标题会让每一条消息都多几行噪音。
- 说明是**用户自己写、要发到 ChatGPT 去的**，所以界面上必须写清楚这一点，
  不能让它看起来只是个本地备注。
- **加列必须走 `migrate()`。** `CREATE TABLE IF NOT EXISTS` 对已存在的表**什么都不做**，
  所以写进 `SCHEMA` 的新列只对新建的库生效 —— 老库会缺列，然后所有碰它的查询都炸。
  `PRAGMA table_info(ssh_hosts)` 查一下再 `ALTER TABLE`，并且要**幂等**（每次开库都跑）。
  改完拿一个"旧 schema 建出来的库"实测一遍：老数据还在、新列有了、再开一次不报错。
- **`upsertSshHost` 不许碰 `note`。** 它的 `ON CONFLICT DO UPDATE` 只列了连接相关的列，
  这正是重连不会冲掉用户写的说明的原因 —— **不要图省事改成整行覆盖**。
- **再提醒一次：`SCHEMA` 是模板字符串，里面的 SQL 注释不能出现反引号。**
  这条已经踩过一次，加 `note` 列时又踩了一次 —— 反引号会把模板字符串提前截断，
  报出来的是 `TS1005: ',' expected`，跟 SQL 一点关系都看不出来。

### SSH 接管模型命令（连上就跑在远端）

- **SSH 一连上，模型的命令就改在远端执行；断开立刻回落本地。** 这是"连上就接管"，
  不是可选开关。理由不是顺手：提示词会把远端平台告诉模型，**如果命令仍在本地跑，
  模型写出的 Linux 命令就会被 PowerShell 执行** —— 提示词与实现对不上时，
  错的不是"不够准确"，而是会**主动把模型推向错误做法**。
  `CommandRunner.ensureShell()` 每次调用都重新问 `deps.remoteShell()`，
  **不要把这个答案缓存起来**（会话随时会来会走）。
- **`remoteShell` 是唯一真源：命令路由和提示词都读它。** 两处各自判断迟早会分叉，
  而分叉的表现是"模型以为在服务器上，命令跑在你自己的电脑上" —— 最坏的那种 bug。
  `SshManager.execShell()` 用 `alive` 判定，`getState()` 里的 `remoteExec` 也是派生的，
  **不要把它们存成字段**：通道随时会死，存下来的 `true` 会让 UI 和提示词一起撒谎。
- **一个连接要开两条通道，用途不同，不要合并：**
  - 交互式 PTY（`client.shell()`）：**给人用的**，就是左边那一片你在敲的东西。
  - `bash -s` exec 通道（`client.exec()`）：**给模型用的**，`RemoteShell` 挂在上面。
  - 不复用 PTY 是因为它会把每个字节回显、并在命令之间打印提示符，
    要从中剥出真输出就只能靠猜提示符长什么样。exec 通道没有回显也没有提示符，分帧是精确的。
  - **代价是两条会话不共享工作目录**（在面板里 `cd` 不影响模型的 shell）。
    这个事实**显示在界面上**（"模型目录 …"），不要藏起来，也不要假装它们是一个会话。
- **远端命令的封装必须三件齐全**（`buildEnvelope`，已用真 bash 跑过 64 项断言）：
  - 尾部 `printf` 是完成标记，**带会话令牌 + 递增序号**；序号对不上的标记当普通输出丢弃
    （命令输出里可能出现任何文本，这条和本地 PowerShell 那边是同一个理由）。
  - `$?` 紧跟在块后面，拿到的才是块的状态而不是 `printf` 的；`$PWD` 一起带回来，
    因为 cwd 跨命令保留、模型必须知道自己在哪。
  - **`} </dev/null` 是防失步的，不是装饰。** 协议本身就活在这条通道的 stdin 上，
    一条 `read`、`cat` 或任何要交互的安装脚本会把下一条命令吃掉。
    花括号是 shell 分组而不是子 shell，所以其中的 `cd`、变量赋值、函数定义仍然留在会话里 ——
    **这正是"常驻会话"的全部意义**，改成 `( ... )` 或 `bash -c` 就把这个能力丢了。
  - 已知边界：命令里出现**不配对的 `{` / `}`** 会让分组解析失败，靠空闲超时兜底。
- **远端探测走同一套后端，不要另开代码路径。** `runEnvironmentProbe()` 返回
  `{kind, result}`：探测打给谁、结果交给哪个解析器，**必须来自同一次调用**。
  分开读的话，后端在两次调用之间变了，就会拿远端的输出喂本地解析器，
  产出"对错误的机器很自信的描述"。
- **远端解析失败时绝不能拿本机信息顶上。** `parseRemoteEnvironment` 读不到就返回空字段，
  只保留主机名和地址（那两个来自连接表单，是真的）。把 `os.arch()` 填进去是
  **对着别人的服务器说谎**，模型会照着 x64 去挑 ARM 的包。本地探测反过来 ——
  Node 兜底比纯猜测强，那里就该兜。
- **`kind` 决定提示词用哪一版，不是 `remote` 字段。** Windows 版和 POSIX 版是真两份
  （词汇表整个换掉：cmdlet、`$env:`、`-Recurse` 全都不适用）。两版**共享
  `OUTPUT_FORMAT_SECTION`** —— 那是应用解析的契约，抄成两份迟早分叉，
  然后其中一种方言会静默地不再往返，看起来像模型不听话。改格式要求只改那一处。
- **远端提示词里点名 PowerShell 命令是有意的**（"Get-ChildItem、`$env:` 在这里都不存在"）。
  对话历史里堆满了 PowerShell，不做显式否定模型会往回退。别为了"干净"把这些名字删掉。
- **主机名和地址绝对不能进提示词。** `remoteName` / `remoteTarget` 是**只给 UI 用**的
  （`describeTarget()` 显示在设置里），`buildPosixPrompt` 里一个字都不能插。
  - 提示词是**每条消息都会贴进 ChatGPT** 的，写进去就等于永久留在第三方服务上了。
  - **主机名往往比地址更敏感**：用户是按用途起名的，"梯子服务器"这种名字直接说明了性质。
    地址 + 端口则等于把那台机器指名道姓地交出去。
  - 模型从"是哪台机器"里得不到任何可操作的信息 —— 它要的是"能写什么命令"，
    而"Linux + bash + 当前目录"已经完全回答了。传输方式是本应用的实现细节，不是任务的一部分。
  - **连"这是远端主机"都不要写。** 这里曾经保留过一句匿名的
    「你操作的是通过 SSH 连接的远端主机」——**用户的判断是对的，模型不需要知道传输方式**。
    "Linux + bash + 当前目录"已经说清了它能写什么命令，多一句就是在描述本应用的实现。
    **加新的环境字段前先问一句：这会不会跟着提示词发出去？**
  - 回归检查：拿一个独特的主机名/地址构造 `EnvironmentInfo`，断言生成的提示词里
    既没有那个名字、也没有 IP、也没有 `user@host:port` 形状 —— 这个测试必须留着。
- **SSH 面板显示时，模型自己的命令行必须镜像进去**（`deps.onRemoteLine` / `onRemoteOutput`，
  在 `CommandRunner.appendLine` / `appendOutput` 里按后端判断）。面板此时渲染的是 SSH 记录，
  不镜像的话**恰好在用户最需要盯着的时候，模型跑的命令和输出是看不见的**。
- **`dispose()` 必须 settle 在飞的命令，不能只 `clearPending()`。**
  `run()` 的调用方在 await 这个 promise，丢掉它会让整条自动化链停在那里、
  执行记录永远停在 `running`、屏幕上一句话都没有。`ConversationShell` 和 `RemoteShell`
  两边都修过了 —— 新写后端时别再把这条漏掉。
- **`RemoteShell` 那条通道死了要自动重开**（`onClosed` → `openExecChannel`，
  上限 `MAX_EXEC_ATTEMPTS`，`this.client !== client` 判断是否已被用户断开）。
  `RemoteShell.dispose()` 会先把自己标记成 disposed，所以拆连接时不会触发重开。
  - **已知取舍**：重开的那一个 RTT 里 `execShell()` 是 null，命令会短暂回落本地。
    循环是回合制的（模型一次往返好几秒），窗口极小；但**不要**为了让这个窗口消失
    而去"保持"一个已经死掉的后端 —— 那会把命令发给死通道。
- **exec 通道起不来时要大声报错**（`remoteExec: false` + 红色错误行 + 琥珀色角标
  "模型命令仍在本地"）。此时提示词仍是本机那份，所以模型不会写 Linux 命令 ——
  但用户必须知道"我连上了"并不等于"命令跑在那边"。
- **界面上必须能一眼看出命令会在哪台机器上跑**：终端面板的角标、状态栏的"远端执行"、
  设置里的"执行目标"。连上 SSH 会静默改掉每一条命令的落点，
  **这是用户唯一不能猜错的事**。

## 终端自动化（会真的在用户机器上执行命令）

- **执行后端是 Windows PowerShell，而且一个会话一个常驻进程**（不是每条命令一个进程）。
  启动时 `spawn(powershell, ['-NoProfile','-NonInteractive','-ExecutionPolicy','Bypass',
  '-EncodedCommand', base64_utf16le(包装脚本)])`，包装脚本是个 `while` 循环一直从 stdin 读命令。
  **这样变量、函数、模块、`pushd`、cwd 全部跨命令保留**，和真终端一致 ——
  改成"一条命令一个进程"会丢掉这些，不要再退回去。
- **命令必须走 base64 单行，不要改回明文或 `-Command`。** 两个理由都是踩过的坑：
  - cmd 时代通过 stdin 管道送明文命令，管道要过代码页、而 `chcp` 管不到管道，
    结果**任何含非 ASCII 的命令都会让 cmd.exe 直接退出**。base64 只有 ASCII，不存在代码页。
  - PowerShell 自己的命令行解析会搞坏 `-Command` 里的引号；base64 完全绕开引号规则。
- **分帧必须带会话令牌 + 递增序号**（`__CT_DONE_<token>_<seq>__ <code> <cwd>`），
  不能只用一个固定哨兵字符串 —— 命令的输出里可能出现任何文本，**序号对不上就当普通输出丢弃**。
  会话就绪另有 `__CT_READY_<token>__`，主进程等它出现才发第一条命令。
- **执行命令期间必须把 stdin 换成空 reader**（包装脚本里 `[Console]::SetIn($ctNullIn)`，
  跑完在 `finally` 里换回来）。否则命令里一个 `[Console]::In.ReadLine()` 就会
  **吃掉下一条命令的 base64 行**，整个会话失步。`-NonInteractive` 让 `Read-Host` 直接报错而不是提示。
  **原生 exe 读的是 OS 句柄，换不掉** —— 那种情况靠空闲超时兜底：杀会话、自动重开。
- **会话死掉要能自愈**：`sessionLost`（`exit`、崩溃、被杀）必须和超时分开报，
  下一条命令自动重开，并且**用记住的 `cwd` 启动新会话**，否则模型会莫名跑到别的目录。
- 包装脚本可以被独立验证：它只是纯 PowerShell + stdin 协议。把 `buildWrapper` 从 `shell.ts`
  里抠出来、起一个真的 powershell 对着喂 base64 就能测，**不需要启动 Electron**。
  改协议时先这样验一遍再交付 —— 这轮 10 项协议断言就是这么过的。
- **包装脚本里执行命令必须走 `| Out-String -Stream | ForEach-Object { Write-Output $_ }`，
  这不是装饰，是防死锁的。** `Get-Location` 和 `$PWD` 返回 `PathInfo`，
  让控制台宿主直接渲染它会**永久阻塞**：
  - 单独跑 `powershell -EncodedCommand Get-Location` 正常输出表格并退出；
  - 但在**一个自己占着 stdin 的循环里**跑同一条命令，**永不返回**（进程活着，不报错）。
  - 其余命令两种写法都正常：表格类（`Get-ChildItem`、`Get-PSDrive`）、对象类（`Get-Date`）、
    字符串、赋值、`Test-Path` 全部 OK。**只有这两个**。
  - **而这恰好是模型开新会话时最先跑的两条命令**（确认自己在哪），所以不加这条加固，
    新会话第一条命令就卡死。这个 bug 真发生过。
  - 试过但**无效**的假设（别再走一遍）：`$Host.UI.RawUI` 显式设尺寸、`$ProgressPreference`、
    去掉 `-NonInteractive`、表格宽度查询 —— 都不是原因。
- **超时靠「空闲」判定，不是固定时长。** 两个计时器：
  - `IDLE_TIMEOUT_MS`（2 分钟**没有任何输出**）→ 判定卡住并终止。**任何输出都会重置它。**
  - `MAX_RUNTIME_MS`（30 分钟绝对上限）→ 防"一直有输出但永不结束"。
  - 原来的固定 60 秒是错的：`npm install`、编译这类**正常且一直在推进**的任务远超 60 秒，
    却会被误杀。改成看输出之后，干活的命令不会被杀，卡住的才会。
  - 没有 PTY，无法直接检测"在等输入"，**沉默是唯一可用的代理信号**，而且很好用。
  - `timedOut` 的类型是 `false | 'idle' | 'ceiling'`（不是 boolean，真值判断仍然可用）。
    **两种情况必须给模型不同的解释**：idle → "标准输入是空的，不要写需要交互输入的命令"；
    ceiling → "把任务拆小"。只说"超时"什么都教不了它。
- **优先 `pwsh.exe`（PowerShell 7），回退 `powershell.exe`（5.1）。**
  7 默认 UTF-8 且支持 `&&`，但 7 不是 Windows 自带，所以两者都可能遇到。
  **不要假设是哪一个** —— 探测到的版本决定提示词里 `&&`/`||` 那条规则怎么写
  （见下面动态提示词那条）。以前写死"教 5.1 兼容语法"，在装了 pwsh 的机器上等于白白
  限制模型，已经废弃。
- **编码策略：边界上统一转成 UTF-8。** 应用内部（UI、数据库、回传给模型的消息）只用 UTF-8。
  - **PowerShell 自己的输出可以确定性设成 UTF-8** —— 脚本开头
    `[Console]::OutputEncoding = [Text.Encoding]::UTF8`。**这是 cmd 做不到的**：
    `chcp` 只改控制台代码页，而写管道的程序根本看不到控制台。
  - **但它调用的外部程序写管道时仍然用 ANSI 代码页（本机 936），这个换不掉** ——
    进程启动时 `GetACP()` 就定死了，除非改系统区域设置。所以三种编码都要认：
    **UTF-16LE**（`wmic` 类工具重定向时的行为，ASCII 字符后面跟 `00`）、**UTF-8**、**GBK**。
  - **先判 UTF-16LE，再按严格 UTF-8 试，失败才整块按 GBK 解。** 顺序不能乱：
    只有严格 UTF-8 通过才敢当 UTF-8，否则 GBK 文本会被误判。多字节被读边界切断的尾巴要留住
    （`splitIncompleteUtf8`；UTF-16 则留奇数尾字节），否则每次读边界都会误判。
  - spawn 时传 `PYTHONUTF8=1` / `PYTHONIOENCODING=utf-8`，让 Python 脚本原生吐 UTF-8。
  - **不要写死成任何一种编码**，也不要去掉 GBK 兜底 —— `certutil` 这类工具真的只会吐 GBK。
- `rejected` 表示命令根本没跑（终端忙 / spawn 失败），要和真实退出码区分开。
- 执行记录的 `status`：`rejected → skipped`、`timedOut → timeout`、`非零退出码 → failed`、
  `退出码 0 → done`。
- 退出码为负时要按十六进制一起显示（`formatExitCode`）：`-2147024816` 就是 `0x80070050`，
  只给十进制根本没法查。
- **`DANGEROUS_PATTERNS` 命中后只入库为 `blocked`、并且仍然执行（auto 模式下）。**
  auto 模式是用户明确的"模型要什么就跑什么"，危险规则**不拦**它 —— 只打一行**醒目的红色警告**
  （`kind: 'error'`，带规则名 + **匹配到的原文片段**）。`暂停` 是唯一的急停。
  手工模式下本来就每条都手点，`blocked` 只是给 UI 一个标记。
- **危险规则的提示必须带上「命令里的哪一段命中了」。** 只报规则名的话，命中就无法回溯验证 ——
  用户会看到一条明显不含 `del /f` 的命令被报「命中 del /f /q」，双方都无法判断是规则误伤
  还是另一条命令，这个争论真的发生过。`findDanger()` 返回 `{label, fragment}`，不要退化。
- **危险规则必须两种方言都覆盖。** 表里既有 PowerShell 的，也有 `rm -rf` / `mkfs` /
  `dd of=/dev/` / `userdel` / `chmod 777` 这些 POSIX 的。只列 Windows 的话，
  **远端那条路就完全没有审计线索**，而远端恰恰不是用户自己的机器。加规则后跑一遍
  `findDanger` 的正反例（它有 25 条规则，很容易写出误伤）。
  - **引号里的命中是有意保留的**：`grep "shutdown"` 会报警，但 `sh -c "rm -rf /"` 一样危险，
    先剥引号再匹配会漏掉后者 —— 漏报比误报糟得多。因为有 `fragment` 回显，
    用户一眼能看出那是搜索词，代价可以接受。
- **执行模式只有两档：`manual`（默认）/ `auto`，存在 `settings` 表的 `executionMode` 键。**
  它只决定"下一条命令要不要自动跑"。
- **结果回传只看 `paused`，不看模式。** 手动模式下点「运行」跑完的结果同样要回传，否则单步流程断掉。
  之前把回传挂在 armed 上，导致手动运行的结果永远发不回去。
- **auto 模式下没有任何闸门，检测到就执行。** 判据只有 `mode === 'auto' && !paused`。
  这是用户的明确原则，而且理由成立：**跑错了会把错误输出回传给模型、模型自己会改；
  拦住不跑则什么都产生不了，循环直接卡死。** 所以：
  - 危险规则**不拦**（只打红色警告，带匹配片段）
  - 历史命令**不拦**（只说明"这是历史命令"）
  - JSON 不合法**不拦**（三级兜底解析照跑）
  - 剩下的刹车只有两个：**手动模式**和**暂停**。
  - **已知后果**：auto 模式下启动应用或打开旧对话，它的最后一条命令会立刻执行。这是刻意的取舍。
- **`live` 不再是闸门，只是日志信息，而且绝不"用掉就清零"。**
  清零的时机只有两个：`armBaseline()`（真实切会话）和页面加载时的 `configure`。
  以前在解析出命令时清零，结果任何一次「提前的静默期」（ChatGPT 先渲染 assistant 回合再填文本）
  都会把标记吃掉，真回复就被误判成历史 —— 这个 bug 反复出现过，不要再退回去。
- **解析失败只在回复"写完"时才报。** 判据是花括号配平（`{` 数 > 0 且等于 `}` 数）。
  模型刚吐 `{"command":"` 时 Brace 不配平 = 还在流式输出，这时报「无法解析」是纯粹的假警报
  （真出现过）。不配平就静默返回，等它写完了下一次静默期自然会解析成功。
- **JSON 必须让模型放进 ```json 代码块里，这不是洁癖，是防字符丢失。**
  ChatGPT 会把回复**按 Markdown 渲染**，而我们是**从渲染后的 DOM 读文本**的
  （`node.innerText`），所以 Markdown 会先吃掉一批字符：
  - `_` 是斜体标记。命令里出现**成对**的下划线就会被当斜体，
    **两个下划线一起消失**。`$_.Name ... -or $_.Command` 渲染后变成
    `$.Name ... -or $.Command` —— 正好是那个坑：模型写对了，是我们拿到的是坏的。
    `$_` 只出现一次时不会配对，所以这个 bug 要等到命令里有**两个** `$_` 才暴露。
  - `*` 同理（`*.txt` 和另一处的 `*` 会配对成加粗/斜体）。
  - **代码块里 Markdown 不生效**，所以围栏是唯一可靠的保护。
  - 解析器本来就剥围栏（`replace(/```[a-zA-Z0-9_-]*/g, '\n')`），裸 JSON 也仍然兼容。
  - **改提示词时不要去掉"必须用代码块包裹"。** 顺带一提，`plain` 代码块和
    `json` 代码块都能过，但写 `json` 能让 ChatGPT 正常高亮、用户看着也正常。
- **`$.` 是命令被 Markdown 吃掉的指纹。** 它在 PowerShell 里任何位置都是非法语法，
  但正好是 `$_.` 的残骸。`MARKDOWN_MANGLED_RE` 命中时要在终端打一行醒目的警告 ——
  **不要试图修复它**（原始字符已经丢了，无法还原），但更不能假装命令是完好的：
  否则模型只会收到「无法将 $.Name 识别为 cmdlet」，完全想不到是文本本身被改了。
- **改执行模型时，必须同步改提示词（`buildWindowsPrompt` / `buildPosixPrompt`）。**
  提示词里对执行环境的描述
  如果和实现不一致，代价不只是"不准确"——它会**主动把模型推向错误做法**。
  真实案例：改成常驻会话后，提示词里还留着"每条命令都是独立进程，变量不会保留，
  需要跨命令记住的内容请写进文件"，于是模型会把中间结果往文件里塞、不敢用变量跨步骤传值，
  而**那正是新架构刚解锁的能力**。改完实现先回头把提示词读一遍。
  同理：让 SSH 接管命令时，提示词必须跟着换成远端平台那份（见上面 SSH 接管那一节）。
- **"新建对话"不算历史恢复。** 新对话的第一条消息会**创建**会话，URL 从 `/` 变成 `/c/<id>`
  发生在**发送之后**。这个判断放在主进程（只有它知道上一个会话 id）：
  `previousConversationId === null && 新 id !== null` → 跳过 `armCommandBaseline()`。
  页面侧的 `armBaseline()` **无条件重置**，不要在里面加"正在等回复就跳过"的逻辑 ——
  那会让"发了消息后立刻切到别的会话"漏掉重置，把新会话的历史判成 `live` 而自动执行。
- **`setMode` 和 `restoreMode` 必须分开。** `setMode` 有"切到 auto 就接上待处理命令"
  （`resumeNewestPending`）的副作用，`restoreMode` 没有。
  **注意：这并不等于启动时安全** —— auto 模式下 `handleDetected` 对新检测到的命令一律执行，
  所以启动后页面上恢复出来的那条旧命令**仍然会被执行**。这是刻意的取舍（见上面 auto 无闸门那条），
  不要误以为 `restoreMode` 提供了保护。
- `MAX_LIVE_SHELLS` 的 LRU 回收现在是**真·资源回收**：一个会话一个常驻 PowerShell 进程，
  不回收就会攒下一堆 PowerShell。回收时**跳过正在跑命令的 shell**。
  `alive` = 会话进程还活着；`running` = 有命令在跑。
- `OUTPUT_FORMAT_SECTION`（`shared/types.ts`）里的 JSON 示例**必须保持合法**（`"description":""`）。
  主进程现在会解析模型回复，示例写成畸形的话模型会照抄，解析直接失败。
  这一段是 Windows 版和 POSIX 版**共用**的，改一次两边都变。
- **JSON 只是驱动终端的协议，不是跟用户说话的方式。**
  - 任务**没完成** → 输出 JSON 让终端执行下一步。
  - 任务**完成了** → **不要再输出 JSON**，直接正常中文回复，说明做了什么、结果如何。
  - 所以"任务结束"的信号是**没有 JSON**，不是 `command` 为空字符串。
    解析器遇到纯文字回复就是"没有命令"，循环自然停下 —— 这是正常路径，不是错误。
  - `handleDetected` 里对 `command === ''` 的处理是**防御性兜底**（老提示词留下的习惯，
    模型偶尔还会吐 `{"command":""}`）。保留它，但不要再在提示词里教这种写法。
  - **纯文字回复绝不能让 `parse-failed` 报警。** 那条诊断的触发条件是「含 `"command":`
    且花括号配平却解析不出来」，正常回复不含 `"command":`，所以安静通过 —— 别把这个条件放宽。
- **提示词是动态生成的，不要改回硬编码。** 启动时用 `DETECT_COMMAND` 真的去问一遍本机
  （`Get-CimInstance Win32_OperatingSystem`），拿到操作系统标题/版本/架构、PowerShell 版本
  与版本类型、**以及当前工作目录**，再交给 `buildTerminalPrompt(env)` 拼装。
  - 硬编码「Windows 11 + Windows PowerShell」是**对机器的猜测**，在 Win10、Server SKU、
    ARM64、装了 pwsh 的机器上都是错的 —— **而模型会照着这个错误信息去写命令**
    （比如在 5.1 上写 `&&`）。
  - 版本相关的规则必须跟着探测结果走：`&&`/`||` 是 PowerShell 7 才有的，
    5.1 上写了直接语法错误。版本读不到时用 **exe 名字**兜底判断（`pwsh*` 说明是 7+），
    **不要因为探测失败就一律当成 5.1**。
  - **探测必须跑在用户看得见的终端里**（`runner.runEnvironmentProbe()`，走 `currentKey()`
    那个会话）。**不要图省事改成一次性 spawn** —— 用户的原始需求就是"启动时打开一个终端
    并用它探测"，隐形的探测等于没做，这条已经踩过一次。
  - **探测不能阻塞启动**（`void probeEnvironment()`）。窗口不等 PowerShell 起来；
    渲染层挂载时会主动拉一次终端状态，所以早于订阅产生的输出也不会丢。
  - **`workingDirectory` 是环境的一部分。** 用户能改终端目录，改完要在同一条链路上重新探测：
    `setTerminalCwd` → 探测 → 重建提示词，**必须串行 await**，否则探测读到的是旧目录，
    提示词里写的也是旧目录。
  - 探测失败要回退到 `FALLBACK_ENVIRONMENT`，不能崩；兜底里架构/版本用 Node 的
    `os.arch()` / `os.release()` 顶上，比纯猜测强。
- **`DETECT_COMMAND` 是单行**，因为它会原样显示在终端里。改多行前先考虑可读性。
- **探测脚本可以脱离 Electron 验证**：把 `DETECT_COMMAND` 从 `environment.ts` 抠出来直接跑，
  或者更彻底一点，通过真实包装脚本发一遍（见上面包装脚本那条）。
  注意它是**字符串数组**，抠出来要 `.join(' ')` —— 忘了的话 `Buffer.from(数组)`
  会把它当字节数组，喂一堆 NUL 给 PowerShell。
- **注入脚本的全局标识符 `__cmdTerminalInterceptor` 是跨文件字面量耦合，改名要两处同改**：
  定义在 `src/main/injected/send-interceptor.js` 的 `STATE_KEY`，引用在 `src/main/embed.ts`
  的 `executeJavaScript` 字符串里。**编译器查不出来** —— 漏改一处，整个注入功能会静默失效
  （页面报告 installed、但所有 sendRaw/armBaseline/checkNow 都变成 no-op）。
  名字里带 cmd 是历史遗留，不影响功能，不建议为了整洁去改。
- **解析模型回复必须三级兜底，严格解析失败绝不能直接放弃**：
  1. `JSON.parse`
  2. `repairJson`（修 `"key","value"` 和尾逗号）
  3. `lenientCommand` → `readJsonishString`：**文本方式抠字段**
  - 模型生成的命令里引号很密集（PowerShell 的 `-Path "C:\a b"`、`Where-Object {$_ -eq "x"}`、
    cmd 风格的 `cmd /c "..."`），它**经常忘记转义**，`JSON.parse` 直接抛异常。
    这时如果返回 null，命令就**无声消失**了 —— 终端里连一行字都没有，看起来就像程序坏了。
    这个 bug 真出现过。
  - `readJsonishString` 的终止判据是「引号后面跟着 `,` 或 `}`」，这是区分真结尾和值内裸引号的唯一办法。
  - **反斜杠只在后面跟着合法 JSON 转义字符时才算转义**，否则原样保留 ——
    不然未转义的 Windows 路径 `C:\Users` 会被吃成 `C:Users`。
  - 三级都失败、且回复已写完时才上报 `parse-failed`（判据见上面那条）——
    **真正的失败绝不允许静默返回**：静默失败和「程序坏了」在用户眼里完全一样。
- 结果回传走 `sendRaw`，**不加系统提示词**，并且在输入框非空时返回 `busy` 而不是覆盖用户草稿。
- 回传的文本必须包含**命令、目录、自然语言结论**三段，再跟输出：

  ```
  命令: <command>
  目录: <cwd>
  结果: 成功 / 失败（退出码 N）/ 执行超时… / 未执行：<原因>

  <输出 或 (命令没有任何输出)>
  ```

  三条理由，缺一不可：
  - **不能只给数字。** 原来只发 `退出码: 未知` + `(无输出)`，shell 崩溃时模型看到的就是这个，
    完全不知道出了什么事、只能瞎猜。结论必须是**人话**，退出码作为补充一起给。
  - **目录必须带上。** cwd 是跨命令保留的，不告诉模型它在哪，它前面 `cd` 过就全忘了。
  - 命令本身也要回显，聊天记录才是一份自带上下文的日志。

  退出码为负时附带 `net helpmsg <低16位>` 的提示，让模型能自己查到 Windows 错误码含义。
- **判断"发出去了"只能看输入框有没有清空，不能看点击是否成功。** ChatGPT 在上一条回复还没结束时
  会忽略发送点击，文字就留在框里、看起来像发出去了。`submitWithRetry` 点击后回查、没清空就重试，
  并把真实结果（`ok`/`stuck`）返回给主进程再告诉用户。不要退化成"点了就当成功"。
